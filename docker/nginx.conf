server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA : toute route inconnue retombe sur index.html (vue-router en mode history).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
